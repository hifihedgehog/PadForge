<UserControl x:Class="PadForge.Views.KBMPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="700"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>   <!-- Keyboard -->
            <RowDefinition Height="2*"/>   <!-- Mouse -->
        </Grid.RowDefinitions>

        <!-- QWERTY keyboard (reuses KeyboardKeyItem.BuildLayout) -->
        <Viewbox Grid.Row="0" Stretch="Uniform" Margin="8">
            <ItemsControl x:Name="KeyboardItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="556" Height="136"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="{Binding KeyWidth}" Height="{Binding KeyHeight}"
                                CornerRadius="3"
                                ToolTip="{Binding Label}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#28888888"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPressed}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource SystemControlHighlightAccentBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding Label}" FontSize="8"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Opacity="0.8" TextTrimming="None"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Viewbox>

        <!-- Mouse visualization -->
        <Viewbox Grid.Row="1" Stretch="Uniform" Margin="8,0,8,8">
            <Canvas x:Name="MouseCanvas" Width="160" Height="200" ClipToBounds="True"/>
        </Viewbox>
    </Grid>

</UserControl>
