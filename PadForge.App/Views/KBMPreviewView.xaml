<UserControl x:Class="PadForge.Views.KBMPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="700"
             Background="Transparent">

    <Grid>
        <!-- The rows are named because the slot surface mode collapses one
             of them (#408). Hiding a Viewbox alone would leave its star row
             holding the space, so the surviving half takes the row height
             too, or Mouse Only draws a mouse in the bottom two fifths under
             an empty gap. -->
        <Grid.RowDefinitions>
            <RowDefinition x:Name="KeyboardRow" Height="3*"/>
            <RowDefinition x:Name="MouseRow" Height="2*"/>
        </Grid.RowDefinitions>

        <!-- QWERTY keyboard — full width -->
        <Viewbox x:Name="KeyboardHost" Grid.Row="0" Stretch="Uniform" Margin="8,8,8,4">
            <Canvas x:Name="KeyboardCanvas" Width="556" Height="136" ClipToBounds="True"/>
        </Viewbox>

        <!-- Mouse — centered below keyboard -->
        <Viewbox x:Name="MouseHost" Grid.Row="1" Stretch="Uniform" HorizontalAlignment="Center"
                 Margin="8,4,8,8">
            <Canvas x:Name="MouseCanvas" Width="160" Height="195" ClipToBounds="True"/>
        </Viewbox>
    </Grid>

</UserControl>
