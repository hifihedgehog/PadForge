<UserControl x:Class="PadForge.Views.KBMPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="700"
             Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>      <!-- Keyboard (majority) -->
            <ColumnDefinition Width="Auto"/>   <!-- Mouse panel -->
        </Grid.ColumnDefinitions>

        <!-- QWERTY keyboard -->
        <Viewbox Grid.Column="0" Stretch="Uniform" Margin="8">
            <Canvas x:Name="KeyboardCanvas" Width="556" Height="136" ClipToBounds="True"/>
        </Viewbox>

        <!-- Mouse panel (body + movement + scroll, vertically stacked, tight) -->
        <Viewbox Grid.Column="1" Stretch="Uniform" Margin="0,8,8,8" MaxWidth="200">
            <Canvas x:Name="MouseCanvas" Width="160" Height="280" ClipToBounds="True"/>
        </Viewbox>
    </Grid>

</UserControl>
